{% extends 'layout.njk' %}
{% block title %}
    <title>Blackwell Academy - {{student.name}}</title>
{% endblock title %}
{% block content %}
    <section id="cadastro">
        <div class="container">
            <form action="/students?_method=PUT" method="POST">
                <div class="card">
                    <div class="image">
                        <img src="{{student.avatar_url}}" alt=""></div>
                        <div class="info">
                            <h3>Editar Aluno</h3>
                            {%include 'students/fields.njk'%}

                            <input type="hidden" name="id" value="{{student.id}}">
                        </div>
                    </div>
                </div>
            </section>
        </form>
    {% endblock content %}